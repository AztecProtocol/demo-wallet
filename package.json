{
  "name": "demo-wallet",
  "private": true,
  "type": "module",
  "scripts": {
    "local-aztec:enable": "node scripts/toggle-local-aztec.js enable && cd app && corepack yarn install && cd ../extension && corepack yarn install",
    "local-aztec:disable": "node scripts/toggle-local-aztec.js disable && cd app && corepack yarn install && cd ../extension && corepack yarn install",
    "local-aztec:status": "node scripts/toggle-local-aztec.js status"
  }
}
